# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# PyInstaller výstupy (viz build/spillway.spec) — build/*.py, *.spec, icon.icns
# jsou SOUČÁST repa, jen samotné sestavené artefakty ne.
build/dist/
build/work/
build/icon.iconset/

# Virtualenv (NIKDY do gitu; navíc bolí v Google Drive — zvaž venv mimo Drive)
.venv/
venv/
.uv/

# Konfigurace a tajemství — NIKDY do repa
config.toml
.env
*.key
*.p12
*.crt
codesign-identity.*

# Lokální data
history.jsonl
*.log

# macOS
.DS_Store

# Whisper modely (stahují se za běhu, ~1,5 GB — nepatří do repa)
models/
*.bin
*.gguf
